<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<!--
  安装前必须替换两个占位符（见 README）：
    __REPO__  本仓库的绝对路径
    __HOME__  你的家目录
  launchd 不会在这些路径里展开 ~ 或环境变量，所以它读到这个文件时必须是字面路径。
-->
<dict>
	<key>Label</key>
	<string>com.yayadesu.auto-network-location</string>

	<key>ProgramArguments</key>
	<array>
		<string>/bin/bash</string>
		<string>__REPO__/wifi-loc-detect.sh</string>
		<string>--apply</string>
		<string>--notify</string>
	</array>

	<!-- 每次网络变化都跑。这里故意没有 StartInterval：这是一个针对特定事件的一次性
	     脚本，不是轮询器。万一漏掉一次 WatchPaths 事件，位置会一直停在错的那个，直到
	     下一次网络变化，所以 README 里写了手动 scselect 的命令。 -->
	<key>WatchPaths</key>
	<array>
		<string>/Library/Preferences/SystemConfiguration</string>
	</array>

	<key>RunAtLoad</key>
	<true/>

	<key>ThrottleInterval</key>
	<integer>60</integer>

	<key>ProcessType</key>
	<string>Background</string>

	<!-- 让 osascript 的通知落进 GUI 会话。 -->
	<key>LimitLoadToSessionType</key>
	<string>Aqua</string>

	<!-- 是字符串而不是整数：launchd 把属性列表的整数按十进制读，所以八进制 077 只能靠
	     这个前导零表达。 -->
	<key>Umask</key>
	<string>077</string>

	<key>StandardOutPath</key>
	<string>__HOME__/.wifi-loc-control/agent.log</string>
	<key>StandardErrorPath</key>
	<string>__HOME__/.wifi-loc-control/agent.log</string>
</dict>
</plist>
